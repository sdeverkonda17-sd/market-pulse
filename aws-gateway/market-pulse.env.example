# Public dashboard origins allowed to call the gateway. Separate additional
# origins with commas. There are deliberately no broker credentials here.
DASHBOARD_ORIGINS=https://market-pulse-sd.netlify.app
PORT=8787
